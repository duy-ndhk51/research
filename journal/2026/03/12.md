# 2026-03-12

## What I Worked On

### SNDQ Company
- Successfully launched **Notes at Table Row** implementation for **Building**, **Contact**, and **Supplier** modules
- Submitted a PR that resolves **out-of-heap memory** error during the Next.js build process
- Investigated slow build script caused by **circular dependency** between React Hook Form and Zod `infer` — also caused by the repo having **different versions of the Zod library**
- Discovered a refactoring approach to **separate logic from UI configs** using a hybrid of **Extract Module-Specific Component** and **Compound Pattern**

### Research Repository
- Indexed more books into the research repo
- Created comprehensive notes on [GreatFrontEnd Front End System Design Playbook](../../02-system-design/greatfrontend-fe-system-design.md) — RADIO framework, evaluation criteria, common mistakes, question types
- Started [Salary Negotiation Leverage](../../10-soft-skills/interviewing/salary-negotiation-leverage.md) draft to document personal strengths for upcoming negotiation

## Business Impact
- 🚀 **Launched Notes at Table Row across 3 modules** (Building, Contact, Supplier) — users can now add notes inline without leaving their workflow, a cross-platform feature delivered in one release
- 🛡️ **Fixed critical production build blocker** (out-of-heap memory) — without this fix, NO new features could be deployed to production. The entire team's delivery was at risk.
- 📈 **Investigating build speed bottleneck** — slow builds affect every developer on the team, every day. Fixing this multiplies the whole team's shipping velocity.
- 📈 **Found scalable architecture pattern** for separating logic from UI — future modules can be built faster with less code and fewer bugs. This is an investment that pays off on every new feature.

## What I Learned
- Circular dependencies between React Hook Form and Zod (especially `z.infer`) can silently degrade build performance and cause heap memory issues — multiple Zod versions in the same repo amplify the problem
- The RADIO framework (Requirements, Architecture, Data Model, Interface, Optimizations) is a structured approach specifically for front end system design interviews, distinct from back-end system design
- Compound Pattern combined with module-specific component extraction can cleanly separate business logic from UI configuration in large-scale form-heavy applications

## Key Insights
- Build performance issues often have multiple root causes stacked together — fixing one (heap memory) can reveal another (circular deps) which reveals yet another (duplicate library versions)
- Front end system design interviews focus on client-side architecture, application state, and UX — not distributed systems. The evaluation criteria heavily weight tradeoff analysis and product sense.

## Next Steps
- Follow up on the heap memory PR — ensure it gets merged
- Continue investigating circular dependency resolution — consider enforcing a single Zod version across the monorepo
- Refine the salary negotiation leverage doc with concrete examples and metrics
- Continue indexing remaining source books
